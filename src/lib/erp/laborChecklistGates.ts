/** Master switch for the two labor-logging gates (turnover quality checklist
 * threshold and daily safety-check approval). Flip back to true to
 * re-enable both the API-side block and the UI warning banners/modal. */
export const ENFORCE_LABOR_CHECKLIST_GATES = false;
